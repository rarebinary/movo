# Canonical plan pointer

The approved and version-controlled plan for Wallspace-level visual polish and the working macOS wallpaper compatibility layer is [`../../ROADMAP.md`](../../ROADMAP.md).

This pointer exists so OMX planning workflows and repository context recovery resolve to the same canonical artifact instead of maintaining two drifting copies.
